Phase		% namespace
battlePhase	% library name
false		% reversed

01 = FirstStrike
0A = PlayerBegin
0D = PlayerEnd
0C = EnemyBegin
0B = EnemyEnd
/%
00 = ? (used by partners)
05 = ? (used by partners)
03 = ? (used by partners)
07 = ? (used by partners)
%/

% the script at <1B379C> 0x80284EBC switches one a number of different phases,
% from 00 to 1E. phase 1E is when Happy Heart executes.
